import "package:flutter/material.dart";

class BusienessScreen extends StatelessWidget {
  const NewsScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text(
     'Busieness',
    );
  }
}
